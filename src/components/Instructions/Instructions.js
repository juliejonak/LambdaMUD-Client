import React from "react";
import { InstructionsWrapper } from "../CustomComponents/index";
const Instructions = () => {
  return (
    <InstructionsWrapper>
      <p>
        Click the arrow keys to navigate through the map. "Say" somthing to
        other players with the input box below.
      </p>
    </InstructionsWrapper>
  );
};

export default Instructions;
